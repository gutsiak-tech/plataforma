VITE_API_BASE_URL=http://localhost:8000
# Legacy fallback (optional): used only if VITE_API_BASE_URL is not set.
VITE_API_URL=http://localhost:8000
VITE_TILES_URL=http://localhost:8080
# 0 = use real API; 1 = use MSW mocks (dev only). Keep 0 for local/prod builds with real data.
VITE_MSW=0
VITE_TREEMAP_MOCK=0
